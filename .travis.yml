language: bash

services: docker

before_install:
  - git fetch --tags
  - docker version

install:
  - docker build -t coatisoftware/local .

script:
  - docker run --name builder coatisoftware/local
